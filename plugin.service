[Unit]
Description=Your plugin
Before=docker.service
After=network.target plugin.socket
Requires=plugin.socket docker.service

[Service]
ExecStart=/usr/lib/docker/your-plugin

[Install]
WantedBy=multi-user.target
